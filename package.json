{
  "name": "lab2_25099433g",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal package.json to stabilize Vercel build (no node dependencies)",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "vercel-build": "echo 'no build'"
  },
  "dependencies": {}
}
